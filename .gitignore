# Maven / Java
target/
*.log
*.tmp
*.out

# IDE files
.idea/
*.iml
.classpath
.project
.settings/
.vscode/

# OS files
.DS_Store
Thumbs.db

# Build artifacts
/backend/target/

# Local data (images, caches, etc.)
backend/src/main/resources/static/images/
data/
*.local.json
dump.rdb

# Environment / credentials (set via env vars or secret manager instead)
.env
.env.*
!.env.example
*.credentials.json
aws-credentials.txt
# Local override with real API keys (never commit)
**/application-local.properties

# -----------------------------------------------------------------------------
# Frontend (Node / Vite / React)
# -----------------------------------------------------------------------------
node_modules/
frontend/node_modules/

# Vite / build output
dist/
frontend/dist/
*.tsbuildinfo

# Vite cache (when not under node_modules)
.vite/
frontend/.vite/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Optional: test / coverage
coverage/
*.lcov
.frontend-coverage/

# Demo / local only (do not sync to GitHub)
docs/DEMO_VIDEO_SCRIPT.md

